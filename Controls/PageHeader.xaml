<UserControl x:Class="UnturnedModManager.Controls.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{DynamicResource PageTitleTextStyle}" />
        <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{DynamicResource PageSubtitleTextStyle}"
                   Margin="0,5,0,0"
                   TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
