<Page x:Class="UnturnedModManager.Pages.ModListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="模组管理"
      Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid Margin="24"
          AllowDrop="True"
          DragOver="Page_DragOver"
          Drop="Page_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Margin="0,8,0,16">
            <TextBlock Text="模组管理"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="管理已安装的 BepInEx 模组，独立启用或禁用每个模组 · 支持拖拽 .dll 一键导入"
                       FontSize="13"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- 状态提示条 -->
        <ui:InfoBar x:Name="StatusInfoBar"
                    Grid.Row="1"
                    IsOpen="False"
                    Title=""
                    Message=""
                    Margin="0,0,0,12" />

        <!-- 工具栏 -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <ui:Button Appearance="Primary"
                           Content="刷新列表"
                           Icon="{ui:SymbolIcon ArrowSync24}"
                           Click="RefreshButton_Click" />
                <ui:Button Appearance="Secondary"
                           Content="打开插件文件夹"
                           Icon="{ui:SymbolIcon FolderOpen24}"
                           Margin="12,0,0,0"
                           Click="OpenPluginsFolder_Click" />
            </StackPanel>
            <TextBlock x:Name="ModCountLabel"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </Grid>

        <!-- 模组列表 -->
        <Border Grid.Row="3"
                CornerRadius="8"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <ListView x:Name="ModListView"
                      BorderThickness="0"
                      Background="Transparent"
                      HorizontalContentAlignment="Stretch"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="16,10"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <TextBlock Text="{Binding FileName}"
                                               FontSize="12"
                                               FontWeight="Light"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               Margin="0,2,0,0" />
                                    <TextBlock Text="{Binding InstallTime}"
                                               FontSize="11"
                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                               Margin="0,2,0,0" />
                                </StackPanel>

                                <ui:ToggleSwitch Grid.Column="1"
                                                 HorizontalAlignment="Right"
                                                 VerticalAlignment="Center"
                                                 IsChecked="{Binding IsEnabled}"
                                                 Checked="ModToggle_Changed"
                                                 Unchecked="ModToggle_Changed"
                                                 Tag="{Binding FileName}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- 空状态 -->
        <TextBlock x:Name="EmptyLabel"
                   Grid.Row="3"
                   Text="暂无模组，请将模组 .dll 放入 BepInEx/plugins/ 目录"
                   FontSize="14"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed" />
    </Grid>
</Page>