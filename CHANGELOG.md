# 📒 更新日志 (Changelog)

本文件记录 UnturnedModManager 启动器的所有版本变更。
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)：主版本.次版本.修订号。

---

## [v2.1.6] - 2026/08/19

### 🏠 首页欢迎区更新摘要
- 修复首页欢迎区在版本号已升级后仍显示旧版固定摘要的问题。
- 每次启动的一次 GitHub Release 元数据检查现在同时读取最新 Release 正文；当当前程序就是 Latest 时，欢迎区会提取并展示 Release 中的更新要点，不会下载任何文件。
- 断网、GitHub 暂时不可用或 Release 没有项目符号时，会回退到 v2.1.6 内置的安全摘要；发现更高版本时仍优先展示更新下载与 SHA-256 校验提示。

### 🧪 验证
- 新增“当前已是最新版本仍读取 Release 更新说明”与“Markdown 项目符号摘要提取”回归测试。

## [v2.1.5] - 2026/08/19

### 📦 GitHub latest Release 社区下载
- 识别 unmod.online 社区条目的 `github_repo` 字段；这类条目会直接读取指定 GitHub 仓库的 `releases/latest`，而非依赖社区中保存的旧包副本。
- 仅接受 latest Release 中唯一的 ZIP 资产，校验资产 URL 必须属于所声明仓库；GitHub API 返回 SHA-256 摘要时，下载完成后再以固定时间比较校验内容。
- GitHub API 限流、网络短时故障或下载失败时，会自动回退到已登录社区账户的既有下载端点；SHA-256 或文件大小不匹配不会降级绕过。
- 插件详情现在明确显示下载来源；解析成功后展示当前 GitHub latest Release 版本与资产大小。安装清单始终记录实际成功下载包的版本，回退社区包时不会误写 GitHub tag。

### 🧪 验证
- 新增 GitHub latest Release 的解析、ZIP 资产选择、下载、大小与 SHA-256 校验回归测试；继续覆盖社区包回退行为。


## [v2.1.4] - 2026/08/16

### 🎨 可读性与侧栏账户体验
- 修复本地插件列表在深色主题下可能沿用控件默认深色文字的问题；插件标题、DLL 文件名和来源标签现在统一使用动态主题文字色，在浅色、深色及各配色方案下保持可读。
- 侧栏账户入口调整为“头像 + 身份 + 用户名”的受限布局；展开时超长身份或用户名会实时显示省略号，悬停可查看完整内容；折叠时只保留居中的完整头像。
- 右下角动态通知的边框颜色现在随当前配色方案同步更新，不再在非默认配色下保留突兀的系统蓝。

### 🧪 验证
- 完成浅色与深色主题下本地插件文字、超长社区身份、折叠侧栏头像以及右下角动态通知的实际界面验收。
- `32/32` 自动化测试通过，并完成 .NET 8 `win-x64` 单文件自包含 Release 构建。

## [v2.1.3] - 2026/08/16

### 🚑 启动修复
- 修复 v2.1.2 首页“启动器更新下载进度”`ProgressBar` 默认使用双向绑定、但目标属性为只读而导致 WPF 初始化阶段直接崩溃的问题。
- 更新进度改为显式单向绑定；已完成 Release 构建、32/32 自动化测试以及实际启动/主窗口创建/正常关闭冒烟验证。

## [v2.1.2] - 2026/08/16

### 👤 社区身份与欢迎体验
- 保存并显示 unmod.online `api/auth/me` 返回的账户 role；侧栏、账户页提示和首页欢迎语会使用“身份 · 用户名”的形式，例如“社区管理员 · YU80Rice”。
- 身份显示只使用服务端实际返回的数据；不会根据昵称、本地插件或其他本地信息推断管理员、创作者等身份。

### 🔔 统一右下角通知
- 登录、插件环境开关、安装、卸载、诊断与账户操作的结果统一改为主窗口右下角动态弹窗；提示会淡入并自动消失，错误提示停留更久，同时最多显示三条。
- 移除各页面重复的页内状态条，避免通知挤占滚动区域或因为页面切换而遗漏操作结果。

### ⬇️ 手动、安全的启动器更新
- 首页欢迎区现在会读取 UMM 官方 GitHub Release 的最新稳定版本；仅当版本高于当前程序时显示“下载并安装”按钮。
- 下载、安装均需用户明确确认；更新文件必须来自官方 Release、大小匹配并通过 GitHub API 提供的 SHA-256 校验，否则不会安装。
- 确认安装后，短生命周期更新帮助器会等待 UMM 退出，再替换 EXE 并保留旧版本 `.bak`；若当前目录无写入权限，保留旧版本并直接启动已校验的新 EXE。

### 🧪 验证
- 新增社区身份本地化、Release 版本解析和 SHA-256 摘要校验回归测试。

## [v2.1.1] - 2026/08/16

### 🦊 首页欢迎区与版本公告
- 首页新增可选的 Q 版吉祥物欢迎区：以透明 PNG 资源呈现，不遮挡游戏启动、插件环境和诊断卡片。
- 新增当前版本更新摘要和“查看完整更新日志”入口；在设置中可随时启用或关闭首页欢迎区与公告。
- 已确认的公告版本会保存到本地配置；程序集版本变化后会自动再次展示新版本摘要，避免用户错过本次更新。
- 公告只基于本地程序版本显示，不会后台联网下载、静默安装或修改游戏文件。

### 🧪 验证
- Release 构建与自动化测试通过；发布包继续使用 .NET 8、`win-x64`、单文件自包含模式。

## [v2.1.0] - 2026/08/15

### 🖼️ 社区详情与图片预览
- 插件封面现可点击打开独立的原尺寸预览窗口，支持滚动浏览、缩放控件、Ctrl + 滚轮缩放和 Esc 关闭。
- 社区详情改为封面、分类与安装状态、作者、版本、文件大小、下载量、点赞量和依赖数量的分区信息布局；没有封面时不会预留空白区域。
- 对插件作者在详情 Markdown 正文中发布的 HTTPS 图片进行安全提取并以缩略图展示，点击可进入同一预览器；不会执行远程 HTML 或脚本。

### 📥 下载与安装任务中心
- 新增侧栏“任务中心”，统一展示社区插件安装、更新、卸载的进度、当前阶段、完成时间、失败原因和尝试次数。
- 社区包下载改为流式读取并显示已接收/总大小及实时百分比；安装器会依次报告目录验证、依赖解析、下载、校验、备份、写入和回滚相关阶段。
- 失败任务可在同一次启动中直接重试；完成和失败记录持久化到用户数据目录的 task-history.json。重启后保留历史与原因，但为避免在过期上下文中操作，需从插件详情重新发起重试。
- 详情页操作完成后会明确提示可在任务中心查看完整记录；既有的文件所有权、冲突阻止、更新回滚和卸载哈希校验策略保持不变。

### 🧪 验证
- Debug 构建通过：0 警告、0 错误。

## [v2.0.3] - 2026/08/15

### 🎨 配色与控件一致性
- 新增“克莱因蓝”配色：浅色模式采用克莱因蓝主强调色与低饱和蓝灰表面，深色模式采用更明亮的蓝色强调色以保证可读性。
- 非默认配色现在会同步覆盖 WPF-UI 的主按钮、已开启开关、选中导航、进度条、焦点环、复选框、单选框与下拉框焦点色；悬停、按下和选中状态也按当前配色计算，不再残留突兀的系统蓝。
- 主题选择继续保存在用户配置中，首次引导与设置页均可选择六套方案：默认 Fluent、暖米白 · UMM 蓝、松林雾绿、深海雾蓝、夜雾紫、克莱因蓝。

### 🧩 实体插件方案
- 新增“插件方案”：可从当前状态创建命名方案、保存当前状态、应用方案或删除方案；每个 Unturned 安装目录的方案数据相互隔离。
- 应用方案时会以 `.dll` / `.dll.disabled` 批量切换插件启停状态；先预检全部文件，任何一步失败会按相反顺序回滚，且禁止在 Unturned 运行时切换。
- 方案是精确启停快照：未被方案包含的现有插件会停用；方案中已经缺失的插件仅提示并跳过。整个过程不会删除 DLL、复制插件、挂载 WinFsp 虚拟盘或改写 BepInEx 配置。

## [v2.0.2] - 2026/08/15

### 🐛 首次启动与隔离验收
- 修复全新用户的窗口位置默认值为 `NaN` 而无法序列化 JSON，导致首次引导的配置不能立即保存的问题；未保存的位置现以 `null` 持久化。
- 支持通过 `UMM_DATA_DIRECTORY` 将配置、社区缓存和诊断启动错误隔离到指定目录；正常用户仍使用 `%AppData%\Roaming\UnturnedModManager`。

## [v2.0.1] - 2026/08/15

### 🎨 前端与交互升级
- 新增多套配色方案：默认 Fluent、暖米白 · UMM 蓝、松林雾绿、深海雾蓝与夜雾紫；可在首次引导和设置中切换。
- 新增首次启动三步引导：选择 Unturned 目录、设置主题并了解社区与本地插件入口；已有有效配置的用户不会被重复打断。
- 折叠侧栏紧凑宽度调整为 88px，账户头像和主题图标拥有稳定的点击区域。
- 社区详情页支持基础 Markdown 排版，依赖条目可直接进入对应详情，并沿用真实来源页返回历史。
- 增加社区依赖本地化显示名称。
- 设置页可重新运行首次设置向导，不会删除插件、账户或已有游戏配置。
- 第二次启动会唤醒已有 UMM 窗口；来自 `umm://install/{id}` 的请求会进入对应的社区详情页，由用户确认安装。
- 设置页改为随主内容区伸展，不再固定为狭窄的左侧列；详情页在封面未加载时不再预留空白大图区域。
- 滚轮从鼠标真实命中的元素路由到最近的可滚动容器；本地插件列表支持 Enter 键打开已匹配的社区详情。

### 🏗️ 架构边界
- 恢复并重构本地运行诊断：可分析 Unity、Unturned、BepInEx 与 DXVK 日志，按崩溃、DXVK/Vulkan、插件异常给出可验证的排障线索，并导出不上传的诊断包。
- 修复虚拟显示驱动被当作主显卡的问题；DXVK 判定会跳过 Oray / GameViewer、远程桌面和常见虚拟适配器，优先选择实际独显。
- 首次启动逻辑迁移到独立 `OnboardingViewModel`，不再把向导状态写进主窗口代码后置事件。
- 新增轻量 Markdown 控件，使用原生 WPF 文本布局，不执行远程 HTML 或脚本。
- 增加单实例保护，避免多个 UMM 进程同时操作同一游戏目录。
- 使用当前 Windows 用户私有的命名管道转交二次启动参数；UMM 仅注册自己的 `umm://` 协议，不覆盖 UML 的 `unmod://` 协议。
- 明确不采用 WinFsp 虚拟盘、挂载式插件切换或挂载式多 profile，避免 Unity 对虚拟文件系统读取的兼容风险。

## [v2.0.0] - 2026/08/14

v2.0 是 UMM 的一次大版本重构。项目继续采用 .NET 8 + WPF，但界面状态、导航、社区访问、本地插件管理和安装行为已从页面事件代码中拆分为可维护的 ViewModel/Service 架构。

### 🎨 界面与交互
- 重构主窗口、游戏启动、本地插件、社区、详情、设置和账户界面，统一加载、空数据、错误及操作反馈。
- 社区首页改为填满内容区域的条目列表；点击条目后进入独立详情页。
- 新增带来源上下文的导航历史：从本地插件跳转到社区详情后，“返回”会回到本地插件页。
- 修复卡片区域滚轮事件被背景控件截获的问题。
- 修复首次启动时侧栏折叠状态、账户入口和主题按钮形态不同步的问题。
- 持久化侧栏、窗口大小、位置、最大化状态以及“浅色/深色/跟随系统”主题。
- 侧栏新增社区账户入口；展开时显示头像与昵称，折叠时显示头像。

### 🌐 unmod.online 社区
- 新增社区列表、缩略图、分类、排序、防抖搜索与自动刷新。
- 新增社区详情页、依赖展示、安装、更新和卸载操作。
- 使用系统浏览器完成登录与人机验证，通过本机 `localhost` 回调接收令牌。
- 保存账户令牌、昵称和头像，并区分“服务器已验证会话”与“仅有本地缓存账户”。
- 新增分类、列表和详情元数据缓存；网络不可用时回退最近缓存。
- 修复本地化文本只能读取、不能写入 JSON 的问题。

### 🧩 本地插件与安装安全
- 将自动安装、升级和修复基线更新为官方 BepInEx 5.4.23.5 win_x64，并增加官方发布包 SHA-256 校验。
- 新增 BepInEx 多源下载回退：已登录时优先使用 unmod.online 社区包，未登录时跳过社区源并依次尝试 `gh-proxy.com`、`ghproxy.net` 国内镜像，最后回退 GitHub 官方；所有来源统一执行 SHA-256 校验。
- 新增“卸载环境”：移除 BepInEx 核心与 winhttp doorstop 文件，同时默认保留 plugins、config、cache、日志和社区安装记录。
- 本地插件支持启用、停用、导入和卸载，兼容玩家手动安装的 DLL。
- 本地条目显示社区标题与 DLL 文件名，并可匹配、跳转和更新对应社区版本。
- 社区安装器新增依赖递归、循环依赖检测、文件所有权清单、冲突阻止、更新回滚和卸载哈希校验。
- 阻止 ZIP 路径穿越及社区包覆盖核心游戏文件，并限制条目数量和解压总体积。
- 安装状态与游戏目录进行协调，避免文件已移除后社区仍显示“已安装”。

### 🏗️ 架构与发布
- 页面后台代码收敛为视图职责，业务逻辑迁移至 ViewModels、Services 和 Models。
- 目标框架统一为稳定版 `net8.0-windows`，正式发布不依赖 .NET 10 Preview。
- 取消安装、修复或启动 BepInEx 时强制部署 `LaunchPerfOptimizer` 与 `WaterPerfOptimizer`。
- 将 `UnturnedModManager.Tests` 正式纳入仓库，覆盖本地化、版本比较和社区缓存行为，并新增 Windows GitHub Actions 构建测试工作流。
- 将程序集、文件和包版本统一升级为 `2.0.0`。

### 📜 许可证与致谢
- 新版本许可证由 MIT 改为 `GPL-2.0-only`。分发 GPL 衍生版本时须继续采用 GPL v2 并提供相应源代码。
- 在 README 和关于页明确致谢 [Ayndpa/unturned-mod-loader](https://github.com/Ayndpa/unturned-mod-loader) 对信息架构与社区交互的启发。
- 将 UML 作者 [Ayndpa](https://github.com/Ayndpa) 列为贡献者，并记录其通过 UMM PR #7 提交的滚轮交互修复。
- 补充 OpenAI GPT（Codex）及其他 AI 协作者在 v2.0 开发中的具体职责。

### 🧪 验证
- Debug 与 Release 构建：0 警告、0 错误。
- 自动化测试：7/7 通过，包含 BepInEx 核心卸载时保留玩家数据、未登录跳过社区源及社区 Cookie 发送的回归测试。
- `win-x64` 自包含单文件发布验证通过，发布产物不再包含两款待整合优化插件。

---

## [v1.6.8] - 2026/07/15

- 新增 GPU 架构检测，根据兼容性启用或关闭 DXVK 默认建议。
- 对兼容性一般的显卡显示 DXVK 风险提示。
- 合并 Ayndpa 提交的滚轮交互修复（PR #7）。

## [v1.6.7] - 2026/07/14

- 移除 DXVK HUD 环境变量注入，避免游戏左上角出现调试信息。

## [v1.6.6] - 2026/07/13

- 修复 Steam Overlay 无法呼出和 DXVK 偶发闪退问题。

## [v1.6.5] - 2026/07/12

- 修复旧版 BepInEx 环境缺少当时内嵌核心模组时的启动异常。该强制部署机制已在 v2.0 移除。

## [v1.6.4] - 2026/07/12

- 将首页已验证的滚轮路由模式应用到关于页。

## [v1.6.3] - 2026/07/12

- 改用 `PreviewMouseWheel` 隧道事件修复卡片区域滚动。

## [v1.6.2] - 2026/07/12

### 🐛 Bug 修复
- **修复首页（HomePage）卡片区域滚轮失效**：BepInEx 状态、全局模组开关、DXVK 优化卡片在鼠标悬停时无法响应滚轮事件，现已通过 `PreviewMouseWheel` 隧道事件 + `VisualTreeHelper` 向上递归寻找最近 `ScrollViewer` 祖先的方式手动路由滚轮偏移。
- **修复设置页（SettingsPage）卡片区域滚轮失效**：应用同样的滚轮穿透修复，覆盖游戏路径选择卡片。

### 🔧 技术细节
- 在 `Pages/HomePage.xaml` 与 `Pages/SettingsPage.xaml` 的最外层卡片包裹容器上订阅 `PreviewMouseWheel="CardPanel_PreviewMouseWheel"`。
- 在对应 `.xaml.cs` 中实现通用 `CardPanel_PreviewMouseWheel` 回调：
  - 通过 `VisualTreeHelper.GetParent` 向上递归寻找 `ScrollViewer` 祖先。
  - 命中后调用 `ScrollToVerticalOffset(VerticalOffset - e.Delta)` 手动应用滚动偏移。
  - 设置 `e.Handled = true` 完成事件截断，防止二次干扰。
- 此方案沿用 v1.6.1 关于页（AboutPage）的修复模式，但适配了"无显式 ScrollViewer 命名"的场景，复用 NavigationView 内部 ScrollViewer。

### 📦 构建产物
- 文件名：`UnturnedModManager_v1.6.2.exe`
- 体积：~70.4 MB（与 v1.6.0 基线一致，启用 `EnableCompressionInSingleFile` + `DebugType=embedded`）
- 类型：自包含单文件（self-contained single-file），无需用户安装 .NET 8 运行时
- 目标架构：win-x64

---

## [v1.6.1] - 2026/07/11

### 🐛 Bug 修复
- 修复关于页（AboutPage）滚轮失效：通过显式 `ScrollViewer` 命名 + `PreviewMouseWheel` 手动滚动。
- 添加 GitHub 仓库链接至关于页。

### 📜 协议同步
- 关于页 GPL-2.0 -> MIT 协议同步。

---

## [v1.6.0] - 2026/07/11

### 🚀 新功能
- **嵌入式核心模组自释放**：启动器自带 `WaterPerfOptimizer_v1.0.dll` 与 `LaunchPerfOptimizer_v1.0.dll`，BepInEx 解压完成后自动释放到 `BepInEx/plugins/`，落地名去版本后缀以避免 BepInEx 重复加载。
- **DXVK 2.4 转译部署**：从 tar.gz 流式提取 `x64/d3d11.dll` + `x64/dxgi.dll`，支持 `.disabled` 后缀切换模式。
- **winhttp.dll 双轨切换**：模组模式 / 原版模式无缝切换，无需重新部署。
- **动态 dxvk.conf**：根据 CPU 物理线程数自适应 `numCompilerThreads`，开启 `enableGraphicsPipelineLibrary` 减少着色器卡顿。
- **DXVK_HUD 注入**：开启 DXVK 时自动注入 `compiler,fps,api` HUD。
- **Steam 路径双轨探测**：注册表 + `libraryfolders.vdf` 遍历。

### 🎨 UI
- 升级至 WPF-UI 3.0.5（Mica 背景 + NavigationView + InfoBar）。
- 主题切换：`Wpf.Ui.Appearance.ApplicationThemeManager.Apply(theme, WindowBackdropType.Mica)`。
- 深色 / 浅色主题持久化至 `AppSettings.ThemeMode`。

---

## [v1.4.0] - 2026/07/08

### 🎉 首次开源发布
- 项目首次开源至 GitHub。
- 实现基础启动器、BepInEx 部署、Steam 路径探测、主题切换。
